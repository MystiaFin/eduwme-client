import MainLogo from "../assets/logo.svg";

const LoadingPage = () => {
  return (
    <div>
      <img src={MainLogo} alt="Logo" className="w-1/2 mx-auto mt-10" />
    </div>
  );
};

export default LoadingPage;
